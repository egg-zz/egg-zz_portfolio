# apps/api

Placeholder for a future backend. Egg Tray entries are stored in the browser (`localStorage`) for now — no server or database exists yet.

When this is implemented, `apps/web`'s `src/features/egg-tray/lib/storage.ts` is the layer to swap from `localStorage` to real API calls.
